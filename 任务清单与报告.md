\# 云巡信链（SkyTrust）无人机跨域协同与可信监管原型系统

\## —— 任务详细清单与完成情况报告



> 报告日期：2026-09-14

> 交付形态：\*\*后端服务\*\*（BACKEND only，全部 API 一律 POST，附 Apifox 可导入文档与样例）

> 代码仓库：本地 git（分支 `main`，HEAD `7c63527`，共 114 次提交；未推送远端）



\---



\## 一、项目概述



「云巡信链（SkyTrust）」是一个\*\*无人机跨域协同与可信监管原型系统\*\*的后端实现，围绕"三条区块链 + 三大业务系统"构建：



| 链 | 角色 | 承载业务 |

| --- | --- | --- |

| \*\*ChainMaker（长安链）\*\* | 监管链 | 跨域可信监管与存证（非旁路：业务链之间必经监管链中转） |

| \*\*Hyperledger Fabric\*\* | 运营链 | 运营方业务（无人机注册证明、任务、许可） |

| \*\*FISCO BCOS\*\* | 管理链 | 管理方职能（审核裁决、许可签发） |



| 业务系统 | 内容 |

| --- | --- |

| \*\*系统一 · 任务申请跨域协同\*\* | 主数据 → 无人机注册跨链证明 → 任务创建（SM9 加密+脱敏）→ 跨链提交 → 监管审核 → 三维冲突协调 → 飞行许可签发/验证/吊销 |

| \*\*系统二 · 链下可信网络\*\* | 亚秒级 Dijkstra 可信路由、SM9 挑战认证会话、消息引擎（seq/SM3/SM9 留痕）、虫洞攻击仿真与 5 维风险检测、节点隔离与可信路径重算恢复 |

| \*\*系统三 · 跨域可信监管\*\* | 6 类安全告警+状态机、7 级跨链身份亚秒追踪（断链即断点，禁止拼造）、监管授权（ChainMaker 上链）、SM9 密文核验（未授权只见封缄，授权后按 scope 解密+双验证+一致性结论上链）、监管审计与驾驶舱 |



\*\*技术栈\*\*：Go 1.25 · Gin v1.12.0 · GORM v1.31.2 + glebarez/sqlite v1.11.0（纯 Go SQLite，无 cgo）· gmsm v0.44.1（国密 SM3/SM9）· OpenAPI 3.0.3（Apifox）。零额外依赖，hermetic（无网络、无 Docker、无链 SDK）环境下可完整构建与测试。



\*\*核心工程指标（当前状态）\*\*：



| 指标 | 数值 |

| --- | --- |

| API 端点 | \*\*64 个\*\*（全部 POST，统一响应结构，`code=0` 为业务成功） |

| 数据模型 | \*\*20 张 GORM 表\*\*（SQLite，AutoMigrate） |

| 顶层测试 | \*\*315 个，全部通过（0 失败）\*\* |

| 验收用例 | \*\*24 例\*\*（TC1/TC2/TC3 各 8 例，独立 :memory: 服务器） |

| 实验类型 | \*\*11 类\*\*（成功率 / p50 / p95 / max 统计 + CSV 导出） |

| 跨链协议 | \*\*13 步协议引擎\*\*、9 态跨链状态机、两跳四段 TxID 全程留痕 |

| 合约工件 | 三链合约/链码源码 \*\*7 份\*\*（ChainMaker×5、Fabric 链码×1、FISCO Solidity×1） |

| 文档工件 | 服务 README（473 行）、合约交付索引、\*\*820 行真实链迁移指南\*\*、Apifox openapi.json（64 端点）+ 测试场景集（24 场景） |



\---



\## 二、里程碑总览（六个开发计划，全部完成）



开发采用"设计规格（spec）→ 实施计划（plan）→ 逐任务实现+独立评审 → 全分支终审 → 合入 main"的流程。每个任务经过\*\*实现者提交 + 评审者独立复核（含独立复跑测试门）\*\*双重把关；每个计划结束时以全量回归测试作为质量门。



| 计划 | 主题 | 任务数 | 状态 | 完成时测试门 |

| --- | --- | --- | --- | --- |

| Plan 1 | 基础平台（工程骨架/响应与错误码/数据模型/状态机/国密/模拟链/审计） | 14 | ✅ 完成并合入 main | 基线全绿 |

| Plan 2 | 跨链网关 13 步协议 + 系统一（任务申请跨域协同，27 端点） | 16 | ✅ 完成并合入 main | 全绿 |

| Plan 3 | 系统二 · 链下可信网络（虫洞攻防，12 端点） | 14 | ✅ 完成并合入 main | 全绿 |

| Plan 4 | 系统三 · 跨域可信监管（密文监管，10 端点） | 9 | ✅ 完成并合入 main | 全绿 |

| Plan 5 | 实验引擎 + 24 例验收套件 + Apifox 文档工件 | 12 | ✅ 完成并合入 main | 274 PASS / 0 FAIL |

| Plan 6 | 真实三链切换（迁移缝架构）+ 硬化 backlog 燃尽 + 合约工件 + 迁移指南 | 22 | ✅ 完成并合入 main | \*\*315 PASS / 0 FAIL\*\* |

| \*\*合计\*\* | | \*\*87 项任务\*\* | \*\*全部完成\*\* | |



\---



\## 三、任务详细清单



\### Plan 1 · 基础平台（14 项，全部完成 ✅）



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 1 | Go 工具链 + 工程骨架 | hermetic Go 1.25 工具链（仓库内 `tools/go`，不入库）、模块与依赖基线冻结 |

| 2 | 配置模块 | 环境变量 + 默认值（`SERVER\_ADDR`/`DB\_PATH`/`SM9\_KEY\_DIR`/`APP\_TIMEZONE` 等） |

| 3 | 统一响应 + 错误码体系 | `code/message/data/trace\_id/timestamp` 统一结构；全局业务错误码单一事实源 |

| 4 | 路由 + 中间件 + 健康检查 | Gin 路由、trace/日志/恢复中间件、`/api/health/\*`、`/api/chain/status` |

| 5 | 数据库 + GORM 模型 | SQLite 连接、AutoMigrate（初始 17 张表，后扩展至 20 张，含审计日志表） |

| 6 | ID 生成器 | 统一业务 ID（MISSION-/PASS-/ALERT-/AUTH- 等前缀）+ 幂等键 |

| 7 | 状态机 | UAV / 任务 / 通行证 / 会话 / 告警 等对象状态机（后扩展跨链 9 态，共 6 类） |

| 8 | 国密 SM3 | 规范化报文 + SM3 摘要（GB/T 32905 标准向量验证） |

| 9 | 国密 SM9 | 签名/验签/加解密 + 主密钥持久化（`data/sm9`） |

| 10 | 商密 API | `/api/crypto/sm3`、`sm9/keygen`、`sm9/sign`、`sm9/verify` 四端点 |

| 11 | ChainAdapter + 模拟链 | 链适配接口与 fabric/chainmaker/fisco-bcos 三模拟链（延迟/故障注入/状态存储） |

| 12 | 演示数据 Seeder | `/api/demo/init`（幂等预置）、`/api/demo/reset` |

| 13 | 统一审计服务 | `/api/audit/query`（分页过滤）、`/api/audit/export`（CSV） |

| 14 | 端到端验证 + README | 基础平台 E2E 测试、服务 README、全量回归 |



\### Plan 2 · 跨链网关 + 系统一（16 项，全部完成 ✅）



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 1 | 遗留修复 A | SM9 密钥加载 `os.IsNotExist` 守卫；模拟链失败回执入库 |

| 2 | 遗留修复 B | Deps 构造期校验、`NewRouter` 返回 error、状态机全覆盖测试 |

| 3 | 共享包 + 跨链消息 | errcode/timex 共享包；跨链消息类型与规范化信封 |

| 4 | 路由策略 + 载荷校验 | 监管链非旁路路由策略（fabric↔fisco 必经 chainmaker）、载荷字段校验 |

| 5 | \*\*13 步跨链协议引擎\*\* | 9 态状态机、幂等去重（2004）、两跳四段 TxID、传输级重试、SM3/SM9 成败均留痕（`verify\_result=PASS\\|FAIL\_SM3\\|FAIL\_SM9`） |

| 6 | crosschain 三端点 + 接线 | `/api/crosschain/send\\|query\\|list`；uavbusiness 服务骨架 |

| 7 | 主数据六端点 | 厂商/运营方注册与列表、航段创建与列表（走廊状态 OPEN/RESTRICTED/CLOSED） |

| 8 | 无人机五端点 | 注册（UAV\_REGISTER\_PROOF 跨链证明→VERIFIED）、查询、列表、状态流转、注销 |

| 9 | 任务三端点 | 创建（描述 SM9 加密落库、对外仅脱敏值）、查询（永不返回密文）、列表 |

| 10 | 任务提交 | 源链交易 + MISSION\_APPLICATION 跨链（fabric→fisco）；失败自动撤回 DRAFT |

| 11 | 审核两端点 | APPROVED/REJECTED/NEED\_COORDINATION 三态裁决 + MISSION\_REVIEW\_RESULT 跨链 |

| 12 | 冲突两端点 | 三维冲突检测（时间∧航路∧高度同时重叠）与协调解决 |

| 13 | 飞行许可五端点 | 签发（FLIGHT\_PASS 跨链）、查询、列表、放行验证（过期自动 EXPIRED）、吊销（本地先行 + PASS\_REVOKE 跨链 best-effort） |

| 13b | 统一时间格式 | `timex.Time` 全响应统一（Asia/Shanghai，双格式解析） |

| 14 | 系统一 E2E | 任务申请→审核→冲突→许可闭环端到端集成验证 |

| 15 | README + 收尾 | 38 端点文档、业务流程说明、全量回归 |



\### Plan 3 · 系统二 链下可信网络（14 项，全部完成 ✅）



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 1 | 错误类型迁移 | errcode.Error 迁移 + crosschain 别名 |

| 2 | offchain 服务骨架 | 服务与 Deps/main/测试夹具接线 |

| 3 | 拓扑 + 可信路由 | 拓扑图构建、Dijkstra 最短路径、`/api/topology/get` |

| 4 | 时延仿真模型 | 确定性时延（通告/实测/地理下限三来源） |

| 5 | 节点域两端点 | `node/register`（四类节点、SM9 身份自动派生）、`node/list` |

| 6 | 会话域三端点 | `session/open`（SM9 挑战认证 + 初始路径）、`close`、`list` |

| 7 | 消息引擎 | `message/send|list`：seq 序号、SM3 摘要、路径时延、失败也留痕、性能统计 |

| 8 | 虫洞攻击开关 | `wormhole/toggle`：隐藏隧道 + 虚假短路径（写库） |

| 9 | 风险检测与隔离 | 5 维虫洞风险评分 `risk/evaluate`、阈值 0.7 自动隔离 |

| 10 | 可信路径重算 | `path/switch`：DEGRADED→RECOVERED 恢复唯一通道 |

| 11 | 事件列表 | `event/list` 虫洞事件（DETECT/ISOLATE/RECOVER）留痕查询 |

| 12 | autopilot | 后台 HEARTBEAT 流量（config 门控，生产默认 2000ms） |

| 13 | 系统二 E2E | 虫洞攻防闭环端到端集成验证 |

| 14 | README 更新 | 系统二 12 端点 + 虫洞攻防流程文档 |



\### Plan 4 · 系统三 跨域可信监管（9 项，全部完成 ✅）



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 1 | regulatory 服务骨架 | 告警/授权 ID 生成器与服务接线 |

| 2 | 告警域三端点 | `alert/raise|list|status`：6 类告警枚举 + 线性状态机（跨级迁移→6002） |

| 3 | \*\*7 级身份追踪\*\* | `trace/identity`：伪名→运营方→…→制造商 7 级跨链亚秒追踪；断链即断点留痕（5001 透传，禁止拼造） |

| 4 | 监管授权两端点 | `authorize/apply|review`：scope×目标×有效窗；APPROVE 上链（ChainMaker，CHAINMAKER- 前缀 TxID） |

| 5 | \*\*SM9 密文核验\*\* | `inspect/ciphertext`：未授权仅返回封缄（密文状态/SM3 摘要/脱敏值，5002）；授权后按 scope 解密临时明文视图 + SM3/SM9 双验证 + 航路/载荷一致性结论（偏航自动告警+去重），核验结论 audit\_hash 上链 |

| 6 | 监管审计两端点 | `regulatory/audit/list|export`（CSV，page\_size 封顶 200） |

| 7 | 监管驾驶舱 | `dashboard/summary`：告警/授权/会话/虫洞事件聚合 + recent\_alerts |

| 8 | 系统三 E2E | 告警→追踪→未授权封缄→授权上链→密文核验→审计导出→驾驶舱→断链留痕全闭环 |

| 9 | README 更新 | 系统三 10 端点 + 密文监管流程文档 |



\### Plan 5 · 实验引擎 + 验收套件 + Apifox 文档（12 项，全部完成 ✅）



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 1 | 网关加固 | FAILED 幂等键 `#rN` 重试缝（RetryOf 溯源）+ 许可吊销跨链重发 |

| 2 | 许可域加固 | ListPass 分页归一、VerifyPass 过期兜底、代签证据测试 |

| 3 | 实验引擎核心 | 统计器（成功率/p50/p95/max）+ 11 类执行器注册表 + Run 编排 worker 池 + 系统一 4 类实验 |

| 4 | 系统二实验 | MESSAGE\_FLOW（NORMAL/ATTACK/DEFENSE 三场景攻防对比）、RISK\_SCAN、STRESS |

| 5 | 系统三实验 | TRACE\_BATCH（条件式黄金线）、INSPECT\_UNAUTHORIZED、INSPECT\_AUTHORIZED、ALERT\_BATCH |

| 6 | experiment 四端点 | `/api/experiment/\*` API 接线与构造校验 |

| 7 | 验收套件 + TC1×8 | 系统一验收 8 例（独立 :memory: 服务器） |

| 8 | TC2×8 | 系统二验收：会话/许可门禁/亚秒交互/虫洞攻防/对比实验 |

| 9 | TC3×8 | 系统三验收：匿名告警/秒级追踪/断链留痕/封缄/授权核验/审计上链/批量追踪 |

| 10 | apidoc 核心 | Schema 推断 / OpenAPI 3.0.3 构建 / 场景文档 / 确定性渲染 |

| 11 | 端点表 + 场景表 | 64 端点（回放故事序）+ 24 场景（验收同源载荷转录） |

| 12 | \*\*Apifox 工件入库\*\* | 回放录制套件 + `docs/apifox/skytrust-backend.openapi.json` + `skytrust-test-scenarios.json` |



\### Plan 6 · 真实三链切换 + 硬化收尾（22 项，全部完成 ✅）



\*\*A. 迁移缝架构（任务 1–7）\*\*



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 1 | ChainTransport 迁移缝 | 网络 I/O 抽象接口（SubmitTx/QueryTx/QueryState/Health）+ 真实传输注册表（RegisterRealTransport/NewRealTransport）；`CHAIN\_MODE=real` 未注册 SDK 时\*\*启动即 fail-fast\*\*，结构上杜绝静默仿真冒充真实链 |

| 2 | 模拟链 TxID 国密化 | TxID 派生 sha256→SM3（前缀契约不变） |

| 3 | 三按链适配器 | chainmaker/fabric/fisco 各自适配器 + 合约/方法白名单（与业务代码固化常量逐字一致） |

| 4 | 分链模式配置 | `CHAINMAKER\_MODE`/`FABRIC\_MODE`/`FISCO\_MODE` 独立覆盖 + `ChainModeFor` 解析 |

| 5 | buildChains 切换 | sim/real 按链组装；real 无传输→fail-fast 并指向迁移指南 |

| 6 | Seeder 解耦 | 依赖 `Resettable` 接口（仅模拟链可重置；真实链永不被重置） |

| 7 | Deps.Chains 统一 | 生产入口与 4 套全栈测试 harness 按链适配器统一接线（行为保持） |



\*\*B. 硬化 backlog 燃尽（任务 8–15，共 15 项历史遗留缺陷全部修复）\*\*



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 8 | F-1/F-3 | 有状态实验钳制串行（消除共享会话竞态）+ setup 失败日志 |

| 9 | F-2/C11 | revokeNeedsResend ctx 与错误检查 + 旧域 WithContext 接线清扫 |

| 10 | F-5/C13 | 全部分页列表 ORDER BY 补唯一 tiebreaker（确定性排序） |

| 11 | C12/C18 | check-then-insert 竞态：事务 + 唯一索引兜底（8 goroutine 恰好一胜者测试） |

| 12 | C16 | 跨链重试入口输入一致性校验（防换参重试） |

| 13 | C17/C19 | ID 年份显式化（跨年翻转稳定）+ 短值（≤4 字符）全脱敏策略 |

| 14 | B3/B4 | 主数据校验对称 + 许可域 UAV 状态跨域守卫（tighten-only） |

| 15 | P3 遗留缝 | 网关重试 failRow 测试缝 + 消息 post-Create 同步失败观测缝 |



\*\*C. 文档与合约工件（任务 16–22）\*\*



| # | 任务 | 交付内容 |

| --- | --- | --- |

| 16 | F-4 | apifox `x-error-sample-source`→`x-sample-source` 重命名 + 全量重生成回放 |

| 17 | ChainMaker 合约源 | 监管链 5 份 Docker-Go 存证合约（regulatory\_record / crosschain\_trace / identity\_mapping / regulatory\_authorization / audit\_record），部署期验证 |

| 18 | Fabric 链码源 | operator\_business 链码（6 方法，recordJSON 约定）+ 独立 go.mod（hermetic 不编译） |

| 19 | FISCO 合约源 | uav\_management.sol（Solidity ^0.8，3 函数 + 3 事件） |

| 20 | 合约交付索引 | `contracts/README.md`：三链角色、合约×方法×常量出处索引（含"预留"标注）、工具链差异一览、hermetic 声明 |

| 21 | \*\*真实链迁移指南\*\* | `docs/real-chain-migration.md`（820 行）：环境清单（7 个链平台 submodule）→ 三链启动与健康检查 → 合约部署 → \*\*精确到文件/行号的代码切换步骤（业务代码零改动）\*\* → CHAIN\_MODE 切换与验收清单 → 回滚与混合模式矩阵 |

| 22 | README 收口 + 终局门 | Plan 6 文档收口；终局门 315 PASS / 0 FAIL（含 `-count=2 -cpu=1` 稳定性复跑） |



\---



\## 四、交付物汇总



\### 4.1 API（64 端点，全部 POST）



分组：health×2 · chain×1 · crypto×4 · demo×2 · audit×2 · crosschain×3 · masterdata×6 · uav×5 · mission×4 · review×2 · conflict×2 · pass×5 · offchain×12 · regulatory×10 · experiment×4。

统一约定：HTTP 恒 200，业务结果以 `code` 为准（0=成功）；`trace\_id` 全链路追踪；`timestamp` Asia/Shanghai。



\### 4.2 代码结构（`services/skytrust-backend/`）



```

internal/api          路由、中间件、统一响应/错误码、64 端点 handler

internal/apidoc       Apifox 文档渲染（Schema 推断 / OpenAPI 3.0.3 / 场景文档 / 确定性渲染）

internal/audit        审计服务（query / export CSV）

internal/chainadapter 链适配接口 + ChainTransport 迁移缝 + chainmaker/fabric/fisco/sim 四子包

internal/config       环境变量配置（含 CHAIN\_MODE 与分链覆盖）

internal/crosschain   跨链网关：13 步协议引擎、信封规范化、路由/载荷策略

internal/crypto       SM3 摘要 + SM9 签名/验签/加解密

internal/demo         演示数据 seeding

internal/errcode      全局业务错误码（单一事实源）

internal/experiment   实验引擎：11 类执行器 + 统计器 + Run 编排 + CSV 导出

internal/model        20 张 GORM 模型 + AutoMigrate + ID 生成器

internal/offchain     系统二：拓扑/Dijkstra、时延仿真、会话、消息引擎、虫洞攻防、autopilot

internal/regulatory   系统三：告警、7 级追踪、监管授权、密文核验、审计、驾驶舱

internal/statemachine 6 类状态机（UAV/任务/许可/跨链 9 态/会话/告警）

internal/timex        统一时间格式

internal/uavbusiness  系统一：主数据/无人机/任务/审核/冲突/许可

cmd/server            服务入口 + buildChains（sim/real 按链组装）

tests/                E2E（foundation/system1/system2/system3）+ acceptance（TC1/TC2/TC3×8）

contracts/            三链合约工件（chainmaker×5 / fabric 链码 / fisco Solidity）+ 交付索引

docs/apifox/          openapi.json（64 端点）+ test-scenarios.json（24 场景）

docs/real-chain-migration.md  真实三链迁移指南（820 行）

```



\### 4.3 Apifox 使用方式



1\. 导入 `docs/apifox/skytrust-backend.openapi.json`（OpenAPI 3.0.3，64 端点，全部 POST，含请求/响应 Schema 与真实样例）；

2\. 导入 `docs/apifox/skytrust-test-scenarios.json`（24 个测试场景，与验收套件同源载荷）；

3\. 样例均由\*\*回放录制套件\*\*从真实运行的服务录制生成（机器校验，非手写）。



\### 4.4 运行方式



```bash

\# 仓库根目录执行（使用仓库自带 hermetic Go 工具链）

export PATH="$PWD/tools/go/bin:$PATH"

export GOPROXY=https://goproxy.cn,direct

cd services/skytrust-backend \&\& go run ./cmd/server     # 监听 :8080



\# 全量回归测试（315 个顶层测试）

go test ./... -count=1

```



首次启动自动创建 `data/skytrust.db`（20 张表）并生成 SM9 主密钥；`POST /api/demo/init` 预置演示数据。



\---



\## 五、质量保障



\### 5.1 测试体系（315 个顶层测试，0 失败）



| 层级 | 内容 |

| --- | --- |

| 单元测试 | 各 internal 包（协议引擎、状态机、国密、路由策略、风险评分、ID 生成、脱敏策略等） |

| 并发测试 | check-then-insert 竞态修复配 8-goroutine "恰好一胜者"测试；`-count=2 -cpu=1` 稳定性复跑通过 |

| 端到端测试 | foundation / system1 / system2 / system3 四条全链路 E2E |

| 验收测试 | TC1/TC2/TC3 各 8 例共 24 例，独立 :memory: 服务器，覆盖需求档案验收条款 |

| 文档回放 | Apifox 工件由回放套件从真实服务录制并机器校验（端点表/场景表与代码同源） |



\### 5.2 开发流程



\- 每个计划：设计规格（spec）→ 实施计划（plan，任务级步骤+测试先行）→ 逐任务"实现者提交 + 独立评审者复核"（评审者独立复跑测试门）→ 缺陷修复轮 → 计划级收尾；

\- Plan 6 另做\*\*全分支终审\*\*（30 提交 / 7830 行 diff 全量阅读）：结论 \*\*0 Critical / 0 Important\*\*，5 项 Minor 均为非阻断打磨项（已记录转入后续 backlog）；

\- 关键不变量全程守护：零新增依赖（go.mod/go.sum 冻结）、验收诚实性（不存在"以模拟链冒充真实链"的断言或文档表述）、参考文档与中间产物不入库。



\### 5.3 真实三链说明（诚实性声明）



当前仓库为 \*\*hermetic 构建\*\*：三条链以进程内模拟传输运行（含延迟与故障注入），\*\*不携带任何链 SDK\*\*；合约工件为部署期验证的源码交付。切换真实三链的路径已完整工程化：



\- 代码侧：`ChainTransport` 迁移缝 + 真实传输注册表已就位，\*\*业务代码零改动\*\*；

\- 运维侧：`docs/real-chain-migration.md` 提供从环境准备、三链启动、合约部署到 `CHAIN\_MODE=real` 切换与验收/回滚的完整手册；

\- 安全性：`CHAIN\_MODE=real` 而未注册真实 SDK 传输时，服务\*\*启动即报错退出\*\*（fail-fast），绝不静默降级为仿真。



\---



\## 六、后续工作（建议）



1\. \*\*部署期\*\*：按迁移指南搭建三链真实网络（Docker / cgo / Java 环境），部署 7 份合约工件，注册各链 SDK 传输，执行"三链真实在线"验收门；

2\. \*\*backlog（终审 5 项 Minor，均非阻断）\*\*：wrapAdapter 未知链显式报错、状态迁移助手 ctx 线化、适配器白名单跨包常量一致性测试、sm9/idgen 派生 sha256→SM3 全面国密化、错误码文档补一行 1001/6002 分域说明；

3\. 前端/演示界面（本项目范围为后端，未包含）。



\---



\*报告完。代码与文档明细可参见：`services/skytrust-backend/README.md`（服务总览）、`contracts/README.md`（合约交付索引）、`docs/real-chain-migration.md`（真实链迁移指南）、`docs/apifox/`（Apifox 导入工件）。\*



