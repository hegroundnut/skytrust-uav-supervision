# 本文件仅为占位指针：真正的运行环境模板在
#   services/skytrust-backend/.env.example   （2.7KB，含三链全部变量与部署注释）
# 用法：cd services/skytrust-backend && cp .env.example .env，按部署实况修改后
#   由 scripts/start-backend-real.sh 加载（.env 已 gitignore，绝不入库）。
# 一键脚本的公共环境变量（链路径/端口）另见 scripts/env.sh。
